services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-brocai}
      POSTGRES_USER: ${POSTGRES_USER:-brocai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-brocai}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-brocai} -d ${POSTGRES_DB:-brocai}"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-brocai}:${POSTGRES_PASSWORD:-brocai}@postgres:5432/${POSTGRES_DB:-brocai}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:8080}
      UPLOAD_DIR: /app/data/uploads
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-8}
      IMAGE_MAX_EDGE_PX: ${IMAGE_MAX_EDGE_PX:-1920}
      IMAGE_JPEG_QUALITY: ${IMAGE_JPEG_QUALITY:-90}
      IMAGE_WEBP_QUALITY: ${IMAGE_WEBP_QUALITY:-90}
      AI_PROVIDER: ${AI_PROVIDER:-mock}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-3.6-flash}
      MAX_AI_IN_FLIGHT: ${MAX_AI_IN_FLIGHT:-20}
      AI_JOB_TIMEOUT_SECONDS: ${AI_JOB_TIMEOUT_SECONDS:-60}
      AI_WORKER_POLL_MS: ${AI_WORKER_POLL_MS:-250}
      AI_JOB_RETENTION_HOURS: ${AI_JOB_RETENTION_HOURS:-12}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - uploads:/app/data/uploads

  frontend:
    build: ./frontend
    ports:
      - "${APP_PORT:-8080}:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  uploads:
