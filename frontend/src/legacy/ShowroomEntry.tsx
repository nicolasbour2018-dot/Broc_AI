// The showroom keeps the pre-redesign stylesheet, loaded only with this chunk. Same order as the old
// app: showroom.css first, then legacy.css (see AdminEntry).
export { default } from '../Showroom'
import '../legacy.css'
